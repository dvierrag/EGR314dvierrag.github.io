# Resources

## Code Files
The final code for this project has been uploaded as part of this repository. It includes all necessary files used to program the microcontroller and run the system.

Key files included:
- `main.py` for main program execution  
- `boot.py` for initialization  
- `config.py` for system configuration  
- Additional folders such as `lib` for supporting libraries  

These files together allow the system to operate as designed and can be reused or modified for future improvements.

## Project Files
All relevant project files have been included to support the hardware and software design. This includes any configuration files and supporting libraries required for proper functionality.

If needed, these files can be downloaded directly from this repository for testing or replication.

